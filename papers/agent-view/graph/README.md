# Graph view of coding-agent literature

This Mermaid graph is an orientation aid only, not a replacement for metadata.

## How to use

- Use it to pick a theme quickly.
- Then open only the relevant topic file under `topics/`.
- If two themes interact (for example, harness + verification), open both files.

Render file: `knowledge-graph.mmd`
